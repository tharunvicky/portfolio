import React from 'react';

// This component has been disabled as part of the refactor to remove the Open Source section.
// It is kept as a stub to prevent build errors if referenced elsewhere, though it should be unused.

const GithubShowcase: React.FC<any> = () => {
  return null;
};

export default GithubShowcase;
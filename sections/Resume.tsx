import React from 'react';

// This component has been disabled as part of the refactor to remove the Resume section.
// It is kept as a stub to prevent build errors if referenced elsewhere, though it should be unused.

const Resume: React.FC<any> = () => {
  return null;
};

export default Resume;